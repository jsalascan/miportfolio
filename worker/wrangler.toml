name = "buzon-contacto"
main = "src/index.ts"
compatibility_date = "2026-07-27"

# El binding nativo solo admite ventanas de 10 o 60 segundos, así que un tope
# por hora o por día no es expresable aquí.
[[ratelimits]]
name = "LIMITADOR"
namespace_id = "1001"
simple = { limit = 2, period = 60 }
